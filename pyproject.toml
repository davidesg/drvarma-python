[build-system]
requires = ["setuptools>=77", "wheel", "cffi>=1.15"]
build-backend = "setuptools.build_meta"

[project]
name = "drvarma"
version = "0.1.0"
description = "Multivariate VARMA modelling — exact maximum-likelihood estimation, forecasting and diagnostics"
readme = "README.md"
requires-python = ">=3.10"
license = "GPL-2.0-or-later"
license-files = ["COPYING"]
authors = [
    { name = "A.B. Treadway" },
    { name = "J.A. Mauricio" },
    { name = "D.E. Guerrero" },
]
keywords = [
    "VARMA", "vector ARMA", "time series", "forecasting", "multivariate",
    "exact maximum likelihood", "econometrics", "impulse response",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Topic :: Scientific/Engineering :: Information Analysis",
]
dependencies = [
    "numpy>=1.24",
    "scipy>=1.10",
]

[project.urls]
Homepage = "https://github.com/davidesg/drvarma"
Repository = "https://github.com/davidesg/drvarma"
Issues = "https://github.com/davidesg/drvarma/issues"

[project.scripts]
drvarma = "drvarma.cli:main"

[project.optional-dependencies]
# Optional CFFI-compiled C engine (P1): pip install "drvarma[c-engine]"
c-engine = ["cffi>=1.15"]
plots = ["matplotlib>=3.6", "pyfug>=2.0"]
# HTML forecast reports (SPS / fuf format); add weasyprint for optional PDF.
forecast-report = ["jinja2>=3.0", "matplotlib>=3.6"]
test = ["pytest>=7"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
